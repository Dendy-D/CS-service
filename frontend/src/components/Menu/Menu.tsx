import React from 'react';

import classes from './Menu.module.scss';

const Menu = () => {
  return (
    <div className={classes.component}>
      <h1>Menu</h1>
    </div>
  );
};

export default Menu;
