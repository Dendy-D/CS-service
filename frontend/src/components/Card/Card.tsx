import React from 'react';

import classes from './Card.module.scss';

const Card: React.FC = () => {
  return (
    <div className={classes.component}>
      <h1>a</h1>
    </div>
  );
};

export default Card;
